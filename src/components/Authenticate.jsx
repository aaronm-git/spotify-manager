import { Container, Row, Col } from "react-bootstrap";

const Authenticate = () => {
  return (
    <Container className="mt-5">
      <Row>
        <Col md={{ span: 8, offset: 2 }}>Authenticate</Col>
      </Row>
    </Container>
  );
};

export default Authenticate;
